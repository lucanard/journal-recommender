# ─── Core (required) ─────────────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
numpy>=1.24.0

# ─── Embedding providers (install ONE) ───────────────────────
# Option A: Local embeddings (free, no API key, runs on CPU)
sentence-transformers>=2.2.0

# Option B: OpenAI embeddings (fast, cheap, needs API key)
# No extra package needed — uses urllib

# Option C: Cohere embeddings (good free tier)
# No extra package needed — uses urllib

# Option D: Voyage AI (recommended by Anthropic)
# No extra package needed — uses urllib

# ─── Data pipeline (for enrichment scripts) ──────────────────
openpyxl>=3.1.0

# ─── Report generation (optional) ────────────────────────────
python-docx>=1.1.0
